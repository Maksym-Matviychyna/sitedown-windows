# SiteDown Windows v1.0.7

Changes:

- Renamed sidebar button `Setup` to `Settings`.
- Changed Settings Key link from `/setup/` to `/settings/`.
- Updated `CURRENT_VERSION` to `1.0.7`.
- Updated `CURRENT_VERSION_CODE` to `7`.
