# Terminology

The app uses the term **Settings Key** everywhere for the key that loads the user's monitoring settings.

The configuration API request body sends:

```json
{ "settings_key": "YOUR_SETTINGS_KEY" }
```
